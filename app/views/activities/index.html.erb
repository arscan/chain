<% @activities.each do |activity| %>
<div>
    <div class="viz_<%=activity.id %>" />
    <%= activity.name %>
    <div class='calendar'>
    <%= calendar do |date|%>
        <%= date.day %>
    <% end %>
</div>
<% end %>

<br />

<%= link_to 'New Activity', new_activity_path %>
